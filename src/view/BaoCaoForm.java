package view;

import javax.swing.*;

public class BaoCaoForm extends JFrame {
    public BaoCaoForm() {
        setTitle("Báo cáo");
        setSize(500,400);
        setLocationRelativeTo(null);
    }
}
